[project]
name = "ts-performance-matrix-python-workers"
version = "0.1.0"
description = "Python worker dependencies for Camunda performance matrix"
requires-python = ">=3.10"

dependencies = [
    "camunda-orchestration-sdk>=8.9.0.dev0",
]
